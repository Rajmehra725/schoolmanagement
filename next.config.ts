/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.pinimg.com'], // yahan allowed domains add karein
  },
};

module.exports = nextConfig;
